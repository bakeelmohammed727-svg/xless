[build]
  command = "npm install"
  publish = "."

[functions]
  directory = "."
  node_bundler = "esbuild"
  included_files = ["dashboard.html"]

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/index"
  status = 200
