[build]
  command = "npm install"
  publish = "."
    
[functions]
  directory = "."
  external_node_modules = ["express", "body-parser", "cors", "request", "serverless-http", "dotenv"]
  node_bundler = "esbuild"
    
[[redirects]]
  from = "/*"
  to = "/.netlify/functions/index"
  status = 200
